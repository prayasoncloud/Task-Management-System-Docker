events {}

http {
  server {
    listen 80;

    location / {
      proxy_pass http://backend:5000;   # Forward traffic to backend
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
    }
  }
}